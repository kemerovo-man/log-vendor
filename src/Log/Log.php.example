<?php

class Log extends \KemerovoMan\LogVendor\Log
{
    // modify me
    public static function test($message, $infoArray)
    {
        static::log('test_file_name', [
            'message' => $message,
            'infoArray' => $infoArray
        ]);
    }

}